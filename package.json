{
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "pack": "npm run clean && build",
    "dist": "npm run clean && build"
  },
  "build": {
    "win64": {
      "appDirectory": "./dist/Entry-win32-x64",
      "title": "Entry",
      "icon": "./icon/app.ico",
      "authors": "EntryLabs",
      "exe": "Entry.exe",
      "loadingGif": "C:/Users/entry_pc/Documents/entry-offline/build/install-spinner.gif"
    }
  },
  "devDependencies": {
    "electron-builder": "^2.8.3",
    "electron-prebuilt": "^0.36.10",
    "rimraf": "^2.5.2"
  }
}
