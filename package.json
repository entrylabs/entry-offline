{
  "private": true,
  "scripts": {
    "start": "npm install && npm run compile && electron ./app",
    "clean": "rimraf dist",
    "pack": "npm run clean && gulp build && gulp install",
    "dist": "npm run clean && gulp build && gulp install",
    "release": "npm run clean && build"
  },
  "build": {
    "win": {
      "appDirectory": "./app",
      "title": "Entry",
      "icon": "./icon/app.ico"
    }
  },
  "devDependencies": {
    "electron-installer-squirrel-windows": "^1.3.0",
    "gulp": "^3.9.1",
    "gulp-electron": "0.0.10",
    "gulp-sync": "^0.1.4"
  }
}
